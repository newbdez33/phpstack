front:
  restart: always
  build: images/nginx
  ports:
    - "80:80"
    - "9000:9000"
  links:
    - memcached:memcached
  volumes:
    - /var/www:/var/www
  extra_hosts:
    - "phpstack_mysql_1:192.168.0.193"

memcached:
  restart: always
  image: tutum/memcached
  ports:
    - "11211:11211"
  environment:
    MEMCACHED_PASS: 12347890
